

1. 编译

make

2. 运行

./output/main

3. python 脚本绘图

python3 plot_results.py

## git提交

0. 拉取远端更新
git fetch
git pull

1. 将文件添加到暂存区

git add .

2. 本地提交

git commit -m "feat[module]： 添加功能"

git commit -m "fix[module]: 修复bug"

git commit -m "refactor[module]: 重构代码"

3. 推送到远端
git push


### 合并分支

1. 切换到对应分支
git checkout <branch>

2. 



## 目录结构

```

 |-build        # 编译产物目录
 |-docs         # 文档
 |-include      # 头文件
 |-output       # 输出结果与数据
 |-src          # 源文件    
```

## 当前计划1

1. 目标跟踪：假设存在两个目标，目标 A 是参考目标，目标 B 是被控对象的目标。其中目标 A 的轨迹是人为设计的参考轨迹，然后希望通过控制目标 B 的运动，使其跟踪到目标 A。
    <!-- a. 定义一个新的对象 A；使该对象的速度呈现正余弦的周期性变化（每个周期乘一个旋转矩阵），就可以使得目标 A 以一个圆周运动。不需要给目标 A，加速度。相当于目标 A 的状态转移函数是一个非线性的函数（旋转矩阵相乘）；
    b. 人为给目标 A 添加测量噪声（模拟传感器的测量过程），不需要添加过程噪声。
    c. 用卡尔曼滤波器处理传感器测量后的目标 A 输出的位置和速度。得到卡尔曼滤波更新后的估计位置和速度测量值（A.y_hat）。 -->
    <!-- d. 将前面卡尔曼滤波更新后得到的估计位置和速度，作为目标 B 控制器的参考值 B.y_ref； -->
    e. 同样利用卡尔曼滤波得到目标 B 自身的估计位置和速度 B.y_hat, B.y_ref - B.y_hat 就是误差。
    f. 将误差输给控制器，并得到控制量。
    g. 打印两个目标的位置和速度，并打印结果。
    h. 尝试绘制出两个小球的轨迹，新任务：尝试画个动画出来，来演示两个目标的运动。

    看不懂

2. 完成 C++ 进阶课程的学习。

上述 DDL：2026-04-09

3. MPC 的公式推导与伪代码思路整理

4. 尝试实现线性二次型的 MPC 的控制器实现

5. 敬请期待

1. 利用 LQR 控制小球绘制圆轨迹；
    a. 如何设计一个目标轨迹（圆轨迹）；
    b. 如何计算目标轨迹与小球当前位置的误差；
    c. 如何控制小球移动到目标位置；
    d. 打印结果





